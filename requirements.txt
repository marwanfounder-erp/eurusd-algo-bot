# Core data / ML
pandas==2.2.2
numpy==1.26.4
scipy==1.13.1

# Live market data — paper/cloud mode (Linux/Railway)
yfinance>=1.2.0

# MT5 — Windows only. Not installed on Linux/Railway (paper mode uses yfinance).
# To install on your Windows trading host: pip install MetaTrader5==5.0.45
MetaTrader5==5.0.45; sys_platform == "win32"

# TA-Lib Python wrapper — requires the TA-Lib C library to be installed first.
# Not required on Railway (pure-pandas RSI fallback is used automatically).
#   macOS:   brew install ta-lib && pip install TA-Lib==0.4.32
#   Ubuntu:  apt-get install ta-lib && pip install TA-Lib==0.4.32
#   Windows: download from https://sourceforge.net/projects/ta-lib/

# Environment / config
python-dotenv==1.0.1
pydantic==2.7.4
pydantic-settings==2.3.4

# HTTP / Telegram / Dashboard
requests==2.32.3
python-telegram-bot==20.7
flask==3.0.3
flask-cors==4.0.1

# Database
psycopg2-binary==2.9.12

# Backtesting / reporting
plotly==5.22.0

# Scheduling
schedule==1.2.2

# Logging
loguru==0.7.2

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7

# CLI
click==8.1.7

# Type stubs
pandas-stubs==2.2.2.240514
